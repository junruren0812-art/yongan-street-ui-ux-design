# 主场景 UI 设计说明

- 页面名称：主场景
- 英文名称：Main Scene
- 当前版本：v0.7
- 文档状态：待评审
- 更新时间：2026-09-10
- Figma：[Main Scene / Wireframe v0.7](https://www.figma.com/design/hSygViz2j9LkQ419P8kHIV/Main-Scene---Wireframe-v0.6?node-id=65-2&p=f)
- 评审输入：[v0.6 Round 01 评审](v0.6/reviews/2026-09-10-round-01.md)
- 修改清单：[v0.7 Round 01 修改清单](v0.7/round-01-action-list.md)

## 1. 设计目标

把 v0.6 已确认的玩法与叙事逻辑转化为可验证的状态线框，确保玩家能发现价目表、完成查看与阅读、形成新认知，并在后续 D104 对话中获得由线索解锁的新行为。

## 2. 状态线框

1. `01_MainScene_Enter`：从地图进入美丽理发店。
2. `02_MainScene_Exploration`：自由观察并选择吴莉或价目表。
3. `03_Object_PriceList_Available`：价目表高亮并显示互动 Icon。
4. `04_Object_PriceList_Selected`：聚焦价目表，一级操作为“查看”。
5. `05_Object_PriceList_Inspection`：进入笔记本式物件信息页。
6. `06_Object_PriceList_AfterRead`：阅读后出现“回忆／对话”操作。
7. `07_MainScene_Exploration_AfterClue`：返回场景并保留已查看／已理解状态。
8. `08_D104_Option_Unlocked`：D104 读取价目表线索并解锁新对话选项。

## 3. 核心交互链

| 状态 | Trigger | Player Action | System Response | New State | Next Available Action |
|---|---|---|---|---|---|
| Enter | 从地图进入理发店 | 按 `Space` 进入 | 显示地点与场景标题 | Exploration | 选择人物或物件 |
| Exploration | 场景加载完成 | 移动焦点 | 可交互对象高亮 | Object Available | 聚焦价目表 |
| Object Available | 指向／接近价目表 | 确认对象 | Icon 与对象名称出现 | Object Selected | 查看或取消 |
| Object Selected | 价目表被选中 | 按 `E` 查看 | 打开物件信息页 | Object Inspection | 继续阅读 |
| Object Inspection | 信息页打开 | 按 `Space` 阅读 | 展示吴莉的内心记录 | After Read | 回忆或对话 |
| After Read | 阅读完成 | 选择后续行为 | 形成新认知并保存状态 | Exploration After Clue | 继续探索 |
| Exploration After Clue | 返回主场景 | 继续探索 | 价目表显示已查看 | Clue Persisted | 进入后续 D104 |
| D104 Unlocked | 进入 D104 且线索满足 | 选择新选项 | 显示由价目表解锁的回应 | Dialogue Branch | 获得进一步信息 |

## 4. 交互与 HUD 规则

- `Exploration`：显示对象提示；`E` 仅在对象选中后出现；`ESC` 返回上一层或取消聚焦。
- `Object Inspection`：隐藏场景对象操作；保留唯一一级确认操作；`ESC` 返回主场景。
- `Dialogue`：仅显示对话选项与对话控制，避免与场景操作并列造成语义冲突。
- 已查看状态返回场景后保留；重复查看不重复触发错误状态。
- 价目表属于物件交互系统，不并入 Dialogue 系统。

## 5. 视觉与适配要求

- 画板基准为 `1920 × 1080`，关键内容位于安全区域内。
- 黄色用于可交互／主要操作，青色用于当前聚焦，绿色用于已查看／已理解。
- Icon 必须与当前对象有明确空间关联。
- “新认知”反馈保持轻量、内心化，不采用写实证物风格。

## 6. 线框图

[查看 v0.7 八状态总览线框图](../../../assets/wireframe/main-scene/v0.7.png)

Figma 页面中保留 v0.6，并新增 `Main Scene / Wireframe v0.7`；八个状态均以独立 Frame 导入，便于继续编辑。

## 7. 待确认事项

- [ ] 物件信息页与 Notebook 是同一系统的不同入口，还是仅共享视觉语言。
- [ ] “回忆”和“对话”的具体触发条件、结果与重复操作规则。
- [ ] 价目表线索在“查看／阅读／回忆／对话”的哪一步正式写入。
- [ ] 吴莉“倾听”和“对话”的行为差异。
- [ ] D104 未解锁选项采用隐藏还是禁用。
- [ ] 手柄与触屏输入是否进入本版本范围。

## 8. 本版变化

- 保留 v0.6 历史版本并新增 v0.7 Figma 页面。
- 完成 8 个最低状态线框，覆盖进入、探索、物件交互、认知保存与 D104 解锁。
- 补齐对象状态、主要操作和反馈链。
- 将仍需产品决策的系统边界与触发条件明确标记为待确认。
